package com.example.isla_beta.models;

import java.util.Date;

public class ChatMessage {
    public String senderId, receiverId, message, dateTime, messageType, encodeImage, videoUrl;
    public Date dateObject;
}
